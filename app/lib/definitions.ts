export type Color = {
    id: number;
    hex_value: string;
}

export type Size = {
    id: number;
    size: string;
    value: number;
}

export type Style = {
    id: number;
    name: string;
}

export type Category = {
    id: number;
    name: string;
}
